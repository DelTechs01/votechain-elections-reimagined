{
  "name": "contract",
  "version": "1.0.0",
  "description": "Smart contracts for VoteChain",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network base",
    "verify": "hardhat verify --network base",
    "test": "hardhat test",
    "baseSepolia-Balance":"hardhat run scripts/check-balance.js --network baseSepolia"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.5.0",
    "hardhat": "^2.22.10"
  }
}